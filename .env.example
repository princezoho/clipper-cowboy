OPENAI_API_KEY=
PROJECT_DIR=
# Port for the production single-process server (npm start / launcher).
# 47474 is chosen because it's unlikely to collide with anything else
# you might be running. Pick something else if you have a clash.
PORT=47474
